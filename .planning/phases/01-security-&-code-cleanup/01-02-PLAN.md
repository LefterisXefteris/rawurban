---
phase: 01-security-&-code-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/cartMutation.ts
  - lib/utils.ts
  - app/page.tsx
  - app/collections/[handle]/page.tsx
  - lib/cartContext.tsx
autonomous: true

must_haves:
  truths:
    - "lib/cartMutation.ts has no 'use server' directive at the top"
    - "formatPrice is exported from lib/utils.ts and imported by both page files"
    - "Neither app/page.tsx nor app/collections/[handle]/page.tsx defines a local fmt function"
    - "lib/query.ts does not exist"
    - "lib/cartContext.tsx has no eslint-disable comments on react-hooks/exhaustive-deps"
    - "npm run build succeeds after all changes"
  artifacts:
    - path: "lib/cartMutation.ts"
      provides: "Cart mutation utilities (plain TS module, no server directive)"
      excludes: '"use server"'
    - path: "lib/utils.ts"
      provides: "cn() + formatPrice() shared utilities"
      exports: ["cn", "formatPrice"]
    - path: "app/page.tsx"
      provides: "Home page"
      excludes: "const fmt"
    - path: "app/collections/[handle]/page.tsx"
      provides: "Collection page"
      excludes: "const fmt"
    - path: "lib/cartContext.tsx"
      provides: "Cart context with properly-wired useCallback hooks"
      excludes: "eslint-disable-line react-hooks/exhaustive-deps"
  key_links:
    - from: "app/page.tsx"
      to: "lib/utils.ts"
      via: "named import"
      pattern: "import.*formatPrice.*from.*@/lib/utils"
    - from: "app/collections/[handle]/page.tsx"
      to: "lib/utils.ts"
      via: "named import"
      pattern: "import.*formatPrice.*from.*@/lib/utils"
    - from: "lib/cartContext.tsx"
      to: "removeItemImpl"
      via: "useCallback dependency array"
      pattern: "\\[removeItemImpl\\]"
---

<objective>
Fix the "use server" directive misuse, extract the duplicated price formatter, delete the empty query file, and replace ESLint suppressions with properly-wired useCallback hooks.

Purpose: These are semantic and structural bugs in the existing code. The "use server" directive on cartMutation.ts incorrectly marks cart utilities as publicly-callable Server Actions. The duplicate fmt function is a maintenance hazard. The eslint-disable comments mask a real React hooks correctness issue.
Output: lib/cartMutation.ts is a plain TS module; lib/utils.ts exports formatPrice; both pages import it; lib/query.ts is gone; lib/cartContext.tsx has no eslint-disable comments and correct useCallback deps.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-security-&-code-cleanup/01-RESEARCH.md
@.planning/phases/01-security-&-code-cleanup/01-01-SUMMARY.md
@lib/cartMutation.ts
@lib/utils.ts
@lib/cartContext.tsx
@app/page.tsx
@app/collections/[handle]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "use server" from cartMutation.ts, extract formatPrice to lib/utils.ts, and update both page files</name>
  <files>lib/cartMutation.ts, lib/utils.ts, app/page.tsx, app/collections/[handle]/page.tsx</files>
  <action>
    **lib/cartMutation.ts:**
    Delete line 1 (`"use server";`). The file becomes a plain TypeScript module. No other changes needed — the file only imports shopifyFetch from lib/index.ts, which uses the standard fetch API and is fully safe in client context.

    Before removing: verify the file has no imports of next/headers, cookies(), or any server-only package. Current imports are only `shopifyFetch` from `./index`. If any server-only import is found, STOP and report — do not proceed with removal.

    **lib/utils.ts:**
    Append the following after the existing cn() function:

    export function formatPrice(amount: string): string {
      return `£${parseFloat(amount).toFixed(2)}`;
    }

    The signature must accept `string` (matching Shopify's priceRange.minVariantPrice.amount type). Do not use Intl.NumberFormat — keep the existing £ format to match current behavior exactly.

    **app/page.tsx:**
    1. Remove line 7-8: `const fmt = (amount: string) => \`£${parseFloat(amount).toFixed(2)}\``
    2. Add formatPrice to the existing import from @/lib/index, OR add a new import:
       import { formatPrice } from "@/lib/utils";
    3. Replace all usages of `fmt(` with `formatPrice(` throughout the file.

    **app/collections/[handle]/page.tsx:**
    1. Remove line 7: `const fmt = (amount: string) => \`£${parseFloat(amount).toFixed(2)}\``
    2. Add formatPrice to the existing import from @/lib/index, OR add a new import:
       import { formatPrice } from "@/lib/utils";
    3. Replace all usages of `fmt(` with `formatPrice(` throughout the file.

    Then delete lib/query.ts:
    Run: rm /Users/lefterisgilmaz/Desktop/raw/lib/query.ts

    Verify no imports reference query.ts:
    grep -r "from.*query" app/ components/ lib/ — should return no results.
  </action>
  <verify>
    grep '"use server"' lib/cartMutation.ts && echo "FAIL - directive still present" || echo "PASS - removed"
    grep "formatPrice" lib/utils.ts && echo "PASS - exported" || echo "FAIL - missing"
    grep "const fmt" app/page.tsx && echo "FAIL - local fmt still exists" || echo "PASS - removed"
    grep "const fmt" app/collections/\[handle\]/page.tsx && echo "FAIL - local fmt still exists" || echo "PASS - removed"
    grep "formatPrice" app/page.tsx && echo "PASS - imported" || echo "FAIL - missing"
    grep "formatPrice" app/collections/\[handle\]/page.tsx && echo "PASS - imported" || echo "FAIL - missing"
    ls lib/query.ts 2>/dev/null && echo "FAIL - file still exists" || echo "PASS - deleted"
    npx tsc --noEmit
  </verify>
  <done>"use server" is gone from cartMutation.ts. formatPrice is in lib/utils.ts and both page files import and use it. lib/query.ts does not exist. npx tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix useCallback ESLint suppressions in lib/cartContext.tsx</name>
  <files>lib/cartContext.tsx</files>
  <action>
    Current problem: removeItemImpl is a plain async function declared inside the component body. Every render creates a new function reference, making it an unstable dependency. Two useCallback hooks suppress the ESLint rule rather than declaring the correct deps.

    Make the following changes to lib/cartContext.tsx:

    1. Move `removeItemImpl` so it is defined BEFORE `updateItem` (currently it is defined between updateItem and removeItem — swap order).

    2. Wrap removeItemImpl in useCallback with an empty dependency array:

       const removeItemImpl = useCallback(async (cartId: string, lineId: string) => {
         setLoading(true);
         try {
           const updated = await removeFromCart(cartId, [lineId]);
           setCart(updated);
         } catch (err) {
           console.error("[Cart] removeItem error:", err);
         } finally {
           setLoading(false);
         }
       }, []);
       // Deps: setLoading and setCart are React state setters (stable by React guarantee).
       // removeFromCart is a module-level import (stable). Empty array is correct.

    3. Update updateItem's dependency array from `[]` to `[removeItemImpl]` and remove the eslint-disable comment:

       const updateItem = useCallback(async (lineId: string, quantity: number) => {
         const cartId = localStorage.getItem("cartId");
         if (!cartId) return;
         if (quantity <= 0) {
           return removeItemImpl(cartId, lineId);
         }
         setLoading(true);
         try {
           const updated = await updateCart(cartId, [{ id: lineId, quantity }]);
           setCart(updated);
         } catch (err) {
           console.error("[Cart] updateItem error:", err);
         } finally {
           setLoading(false);
         }
       }, [removeItemImpl]);

    4. Update removeItem's dependency array from `[]` to `[removeItemImpl]` and remove the eslint-disable comment:

       const removeItem = useCallback(async (lineId: string) => {
         const cartId = localStorage.getItem("cartId");
         if (!cartId) return;
         await removeItemImpl(cartId, lineId);
       }, [removeItemImpl]);

    The two `// eslint-disable-line react-hooks/exhaustive-deps` comments must be deleted entirely — not replaced with other comments.

    Do not change any other logic in the file (addItem, hydration useEffect, CartProvider JSX, useCart, CartContext, CartContextType).
  </action>
  <verify>
    grep "eslint-disable" lib/cartContext.tsx && echo "FAIL - suppress comments still present" || echo "PASS - removed"
    grep "removeItemImpl" lib/cartContext.tsx | grep "useCallback" && echo "PASS - wrapped in useCallback" || echo "FAIL - not wrapped"
    grep "\[removeItemImpl\]" lib/cartContext.tsx | wc -l | xargs -I{} test {} -eq 2 && echo "PASS - two hooks list removeItemImpl as dep" || echo "FAIL - dep count wrong"
    npx tsc --noEmit
    npm run lint 2>&1 | grep -i "exhaustive-deps" && echo "FAIL - lint error remains" || echo "PASS - no exhaustive-deps errors"
  </verify>
  <done>lib/cartContext.tsx has no eslint-disable comments. removeItemImpl is a useCallback with []. updateItem and removeItem both list [removeItemImpl] as their dependency. npx tsc --noEmit passes. npm run lint produces no react-hooks/exhaustive-deps warnings.</done>
</task>

</tasks>

<verification>
Run the full build to confirm all nine requirements close together:

  npm run build

Expected: exits 0 with no TypeScript errors. The build verifies:
- cartMutation.ts without "use server" still resolves correctly from cartContext.tsx
- formatPrice type (string -> string) matches call sites in both pages
- No broken imports from lib/query.ts deletion
- useCallback changes do not introduce type errors

Also run: npm run lint
Expected: no react-hooks/exhaustive-deps warnings in lib/cartContext.tsx.
</verification>

<success_criteria>
1. npm run build succeeds — no TypeScript errors
2. npm run lint — no react-hooks/exhaustive-deps errors in cartContext.tsx
3. lib/cartMutation.ts line 1 is NOT "use server"
4. lib/utils.ts exports formatPrice(amount: string): string
5. app/page.tsx and app/collections/[handle]/page.tsx both import formatPrice from @/lib/utils and have no local fmt function
6. lib/query.ts does not exist
7. lib/cartContext.tsx has zero eslint-disable-line comments; removeItemImpl is useCallback with []; updateItem and removeItem each declare [removeItemImpl] as dep
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-&-code-cleanup/01-02-SUMMARY.md` following the summary template.
</output>

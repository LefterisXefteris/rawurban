---
phase: 01-security-&-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - lib/index.ts
autonomous: true

must_haves:
  truths:
    - "npm run build succeeds with zero TypeScript errors after dep removal"
    - "App throws a descriptive error on startup if SHOPIFY_STORE_DOMAIN is missing"
    - "App throws a descriptive error on startup if STOREFRONT_ACCESS_TOKEN is missing"
    - "Shopify API version appears exactly once as a named constant, not inline in the URL"
    - "package-lock.json is absent from the repo; pnpm-lock.yaml is the only lockfile"
    - "@apollo/server and @shopify/shopify-api are absent from package.json dependencies"
  artifacts:
    - path: "lib/index.ts"
      provides: "Env guard + API version constant + shopifyFetch"
      contains: "SHOPIFY_API_VERSION"
    - path: "package.json"
      provides: "Dependency manifest"
      excludes: "@apollo/server, @shopify/shopify-api"
    - path: "pnpm-lock.yaml"
      provides: "Sole lockfile"
  key_links:
    - from: "lib/index.ts"
      to: "process.env.SHOPIFY_STORE_DOMAIN"
      via: "module-level guard"
      pattern: "throw new Error.*SHOPIFY_STORE_DOMAIN"
    - from: "lib/index.ts"
      to: "SHOPIFY_API_VERSION"
      via: "template literal in shopifyFetch"
      pattern: "\\$\\{SHOPIFY_API_VERSION\\}"
---

<objective>
Remove unused production dependencies, delete the npm lockfile, and harden lib/index.ts with env-var guards and a named API version constant.

Purpose: Establish a clean, secure baseline before any feature work. Unused deps expand the attack surface and increase bundle/install time. Silent env-var failures cause cryptic runtime errors. The inline API version string is a maintenance hazard.
Output: package.json with two deps removed, pnpm as sole package manager, lib/index.ts with fail-fast env guard and SHOPIFY_API_VERSION constant.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-&-code-cleanup/01-RESEARCH.md
@lib/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused deps and establish pnpm as sole package manager</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
    The current node_modules may have been installed by npm (package-lock.json exists alongside pnpm-lock.yaml). Before removing packages, reinstall cleanly under pnpm to avoid lockfile conflicts:

    1. Run: rm -rf node_modules
    2. Run: pnpm install
    3. Run: pnpm remove @apollo/server @shopify/shopify-api
    4. Run: rm package-lock.json

    Both @apollo/server and @shopify/shopify-api have zero imports in app/, components/, and lib/ — confirmed by research. They are safe to remove with no code changes required.

    After removal, verify package.json no longer lists either package under "dependencies".
  </action>
  <verify>
    grep "@apollo/server" package.json && echo "FAIL - still present" || echo "PASS - removed"
    grep "@shopify/shopify-api" package.json && echo "FAIL - still present" || echo "PASS - removed"
    ls package-lock.json 2>/dev/null && echo "FAIL - lockfile still exists" || echo "PASS - gone"
    ls pnpm-lock.yaml && echo "PASS - pnpm lockfile present" || echo "FAIL - missing"
  </verify>
  <done>package.json has no @apollo/server or @shopify/shopify-api entries. pnpm-lock.yaml exists. package-lock.json does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add env-var guards and extract API version constant in lib/index.ts</name>
  <files>lib/index.ts</files>
  <action>
    Current state of lib/index.ts:
    - Line 3: const domain = process.env.SHOPIFY_STORE_DOMAIN;  (no guard — silently undefined)
    - Line 4: const token = process.env.STOREFRONT_ACCESS_TOKEN;  (no guard — silently undefined)
    - Line 74: hardcoded "2024-01" inline in the fetch URL
    - Line 78: uses token! (non-null assertion)

    Make the following changes:

    1. After lines 3-4 (the const declarations), add these guards immediately below them:
       if (!domain) {
         throw new Error(
           "[Shopify] SHOPIFY_STORE_DOMAIN is not set. Add it to .env.local."
         );
       }
       if (!token) {
         throw new Error(
           "[Shopify] STOREFRONT_ACCESS_TOKEN is not set. Add it to .env.local."
         );
       }

    2. After the guard block (before the type definitions), add:
       const SHOPIFY_API_VERSION = "2024-01";

    3. In the shopifyFetch function, replace the hardcoded URL:
       FROM: `https://${domain}/api/2024-01/graphql.json`
       TO:   `https://${domain}/api/${SHOPIFY_API_VERSION}/graphql.json`

    4. In the shopifyFetch headers, replace the non-null assertion:
       FROM: 'X-Shopify-Storefront-Access-Token': token!,
       TO:   'X-Shopify-Storefront-Access-Token': token as string,

    The guards must be at module evaluation time (top level, not inside a function) so the error surfaces on first import regardless of Next.js lifecycle stage (dev, build, start).

    Do NOT rename the file or change any other logic. Do NOT add Zod or any schema library.
  </action>
  <verify>
    npx tsc --noEmit
    grep "SHOPIFY_API_VERSION" lib/index.ts
    grep "throw new Error" lib/index.ts
    grep "token!" lib/index.ts && echo "FAIL - non-null assertion still present" || echo "PASS - removed"
    grep "2024-01" lib/index.ts | grep -v "SHOPIFY_API_VERSION" && echo "FAIL - inline version still exists" || echo "PASS - only in constant"
  </verify>
  <done>lib/index.ts has module-level guards for both env vars, a SHOPIFY_API_VERSION constant, the fetch URL uses the constant, and token! is replaced with token as string. npx tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
Run the full build after both tasks:

  npm run build

Expected: build succeeds with no TypeScript errors. If it fails, check for import resolution errors related to the removed packages (should be none — zero imports existed).

Manual spot-check: temporarily comment out SHOPIFY_STORE_DOMAIN in .env.local and run `node -e "require('./lib/index.ts')"` or start the dev server — should throw the descriptive error immediately, not fail silently later.
</verification>

<success_criteria>
1. npm run build succeeds (no TS errors, no missing module errors)
2. package.json has no @apollo/server or @shopify/shopify-api
3. Only pnpm-lock.yaml exists (no package-lock.json)
4. lib/index.ts throws a descriptive Error when either env var is absent
5. SHOPIFY_API_VERSION = "2024-01" is defined once; URL uses the constant
6. No token! non-null assertion in lib/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-&-code-cleanup/01-01-SUMMARY.md` following the summary template.
</output>
